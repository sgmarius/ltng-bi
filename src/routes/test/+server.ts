// import client from "$lib/data/db"

// export async function GET() {
//   const site = await client.collection('posts').insert({"key": "value"});
//   const posts = await client.collection('posts').findAll().toArray();

//   return {
//     status: 200,
//     body: { posts }
//   }
// }